# ----------------------------
# RESULTS
# ----------------------------
if st.button("Show My IP Recommendation"):

    # Determine top IP type
    top_ip = max(ip_scores, key=ip_scores.get)

    st.success(f"🏆 Recommended IP Strategy: **{top_ip}**")
    st.markdown("---")

    # =============================
    # DESCRIPTION
    # =============================
    st.write("### 📘 Description")
    st.write(rationale_data[top_ip]["description"])

    # =============================
    # NEXT STEPS
    # =============================
    st.write("### 🧭 Recommended Next Steps")
    for step in rationale_data[top_ip]["next_steps"]:
        st.write(f"- {step}")

    # =============================
    # COST ESTIMATE
    # =============================
    st.write("### 💰 Approximate Cost")
    st.write(rationale_data[top_ip]["approx_cost"])

    st.markdown("---")

    # =============================
    # DISCLAIMER
    # =============================
    st.info(
        "This is a guidance tool for early-stage innovators. It does not constitute legal advice. "
        "For formal protection and filing, consult a registered IP attorney or CIPC specialist."
    )
